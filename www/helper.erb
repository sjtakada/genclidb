module <%= @class_name %>Helper
  def get_default(key)
    value = nil
<% if @default_def.size > 0 %>

    case key
<%   @default_def.each do |k, v| %>
    when :<%= k %>
      value = <%= v %>
<%   end %>
    end

<% end %>
    value
  end
end
